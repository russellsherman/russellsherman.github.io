{
  "name": "neverenough-info",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal site for Russell Sherman — neverenough.info",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --host",
    "start": "astro dev --host",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "verify": "node scripts/verify.mjs",
    "test": "npm run build && npm run verify",
    "lhci": "CHROME_PATH=\"$(node -e \"console.log(require('playwright').chromium.executablePath())\")\" lhci autorun"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.6",
    "claude": "^0.1.1",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@lhci/cli": "^0.15.1",
    "cheerio": "^1.2.0",
    "playwright": "^1.62.1",
    "typescript": "^6.0.3"
  }
}
